# Secrets template for Streamlit Cloud
# Copy this file to secrets.toml and add your real credentials
# DO NOT commit secrets.toml with real credentials to git!

[mongo]
uri = "mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/"
db = "BioflocDB"
collection = "SensorReadings"
